
  # PHP + PaaS

  This is a code bundle for PHP + PaaS. The original project is available at https://www.figma.com/design/bdNTpcL1fe08w41JjW0FIu/PHP---PaaS.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  